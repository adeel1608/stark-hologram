Add Orbbec Gemini 336 depth integration + visual polish

MAJOR FEATURES:
- Orbbec Gemini 336 depth camera integration for real Z-axis push/pull
- Bidirectional WebSocket protocol (hand coords ⟷ depth values)
- Auto-detect Orbbec SDK with graceful fallback to placeholder mode
- Blueprint mode (edges-only wireframe) for technical CAD aesthetic
- Consolidated CONFIG section with all tunable parameters

IMPLEMENTATION:

depth_server.py (complete rewrite):
- Auto-detect SDK module (tries 5 variants: pyorbbecsdk, ob, etc.)
- Print detailed installation guide if SDK not found
- Orbbec pipeline: depth (640x400) + color (640x480) streams
- Depth→Color alignment for pixel-perfect hand coords mapping
- Robust depth sampling with 7x7 median filter
- WebSocket bidirectional: receive hand positions, send depth (meters)
- Heartbeat messages prevent browser freeze
- Async/await architecture for 30 FPS streaming

static/app.js (complete rewrite):
- CONFIG SECTION (lines 9-85): all parameters consolidated
- Send hand positions to depth_server every frame (landmark 9)
- Receive and display depth values in HUD
- Integrate depth into Z movement during RIGHT fist MOVE mode
- Baseline capture, smooth interpolation, deadzone filtering
- Safety: disable depth if null/stale (>800ms)
- Visual polish:
  - Orb size reduced by ~35% (more subtle)
  - Blueprint mode toggle (edges-only, no faces)
  - Color cleanup (cyan + amber only)
  - Beam verified straight (linear interpolation)

DOCUMENTATION:
- ORBBEC_INSTALL.md: step-by-step SDK installation for Ubuntu 22.04
- DEPTH_TESTING.md: comprehensive testing & troubleshooting (450 lines)
- RUN.md: quick start guide for running the app
- IMPLEMENTATION_SUMMARY.md: complete technical documentation
- README.md: updated with depth features, CONFIG guide, visual modes

PRESERVED FEATURES (no breakage):
- All existing gestures: fist, pinch, two-hand, palm, thumbs-up
- Snap/docking, orb system, energy beam, laser, HUD labels
- Intent gating, hysteresis, hand smoothing
- Particle system, holographic pulse effects

CONFIGURATION:
All tunable parameters now in app.js CONFIG section:
- Gesture thresholds (pinch, fist, hold frames)
- Movement (XZ plane + depth Z)
- Depth (Z_GAIN, Z_SMOOTH, Z_DEADZONE_M, Z_CLAMP)
- Visual (orb size, colors, beam)
- Snap/docking behavior

TESTING:
- ✅ depth_server.py compiles and imports
- ✅ SDK auto-detect works (shows install guide if missing)
- ✅ Runs in placeholder mode (null depth values)
- ✅ No linter errors
- ✅ All existing features preserved

NEXT STEPS:
1. Install Orbbec SDK (see ORBBEC_INSTALL.md)
2. Run depth_server.py + app.py (see RUN.md)
3. Test depth integration (see DEPTH_TESTING.md)
4. Tune CONFIG parameters to taste

Files changed:
- depth_server.py (complete rewrite, ~330 lines)
- static/app.js (complete rewrite, ~1450 lines)
- README.md (major updates)
- ORBBEC_INSTALL.md (new, 280 lines)
- DEPTH_TESTING.md (new, 450 lines)
- RUN.md (new, 160 lines)
- IMPLEMENTATION_SUMMARY.md (new, 550 lines)
- COMMIT_MESSAGE.txt (this file)

